version: '3.8'

services:
  rag-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
      - ./logs:/app/logs
    environment:
      - ENVIRONMENT=production
    restart: unless-stopped
